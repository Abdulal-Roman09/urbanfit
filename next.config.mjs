/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.ibb.co",
      "images.unsplash.com",
      "images.pexels.com",
      "cdn.pixabay.com",
      "avatars.githubusercontent.com"
    ],
  },
};

export default nextConfig;
